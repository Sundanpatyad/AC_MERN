import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'awakeningclasses.vercel.app',
  appName: 'Awakwning Classes',
  webDir: 'dist'
};

export default config;
